{
  "name": "socketExample",
  "version": "1.0.0",
  "description": "A simple message passing socket example from node args to first server and to second server.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Morteza Nokhodian",
  "dependencies": {
    "socket.io": "^2.2.0",
    "socket.io-client": "^2.2.0"
  }
}
